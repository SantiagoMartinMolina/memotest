import styled from "styled-components";

export const StyledTimer = styled.div`
    text-align: center;
    font-size: 1.3em;
    color: #fff;
    font-weight: 700;
    
    span{    
        display: block;
        font-size: 4em;
        color: var(--primary);
        line-height: 1;
    }
`